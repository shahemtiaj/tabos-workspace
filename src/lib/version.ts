// Single source of truth for the TabOS build version.
// Bump this (and extension/manifest.json) on every release.
export const TABOS_VERSION = "1.1.0";
export const EXTENSION_ZIP = `tabos-extension-v${TABOS_VERSION}.zip`;
